name: Keep Alive

on:
  schedule:
    - cron: '0 0 */20 * *'  # 每 20 天触发一次，防止 schedule 被禁用
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Keep workflow active
        uses: gautamkrishnar/keepalive-workflow@v2
